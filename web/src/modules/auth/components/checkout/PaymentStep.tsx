/**
 * @deprecated O método de pagamento é escolhido após o cadastro, na tela /pagamento.
 * Mantido apenas como referência de formulários por gateway.
 */
export function PaymentStep() {
  return (
    <div className="rounded-lg bg-surface-2 p-4 text-sm text-muted">
      A forma de pagamento é escolhida depois do cadastro, ao pagar a cobrança na plataforma.
    </div>
  )
}
